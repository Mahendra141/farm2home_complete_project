spring:
  datasource:
    url: jdbc:mysql://localhost:3306/farm2home?useSSL=false&serverTimezone=UTC
    username: root
    password: Mahi
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
